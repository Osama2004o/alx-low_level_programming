#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - prints last digit
 *@n: the integer
 * Return: 1 for the last digit
 **/

int print_last_digit(int n)
{
int digit;
digit = n % 10;
return (digit);
}
